/*
const addNums = (num1 = 1, num2 = 1) => {
    return num1 + num2;
}
*/
const addNums = (num1 = 1, num2 = 1) => num1 + num2; // This works like the same function above but with less code.

console.log(addNums(2, 1));